info:
  name: partialUpdateProfile_1
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/users/me/settings"
  body:
    type: json
    data: |-
      {
        "lastSeenSetting": "",
        "friendRequests": "",
        "profileVisibility": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/users/me/settings"
      method: PATCH
      body:
        type: json
        data: |-
          {
            "lastSeenSetting": "",
            "friendRequests": "",
            "profileVisibility": false
          }
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "lastSeenSetting": "",
            "friendRequests": "",
            "profileVisibility": false
          }
