info:
  name: createDirectMessageConversation
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/conversations"
  params:
    - name: userId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/conversations"
      method: POST
      params:
        - name: userId
          value: ""
          type: query
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": "",
            "conversationType": "",
            "createdDate": ""
          }
