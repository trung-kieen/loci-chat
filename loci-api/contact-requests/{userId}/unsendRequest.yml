info:
  name: unsendRequest
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/contact-requests/:userId"
  params:
    - name: userId
      value: "{{user_public_id_1}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/contact-requests/:userId"
      method: DELETE
      params:
        - name: userId
          value: ""
          type: path
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "status": {
              "value": "",
              "connected": false,
              "notConnected": false
            }
          }
