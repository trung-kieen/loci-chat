info:
  name: sendRequest
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/contact-requests/:userId"
  params:
    - name: userId
      value: "{{user_public_id_1}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 201 Response
    description: Created
    request:
      url: "{{baseUrl}}/contact-requests/:userId"
      method: POST
      params:
        - name: userId
          value: ""
          type: path
    response:
      statusText: Created
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "status": {
              "value": "",
              "connected": false,
              "notConnected": false
            }
          }
