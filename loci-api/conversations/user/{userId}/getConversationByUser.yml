info:
  name: getConversationByUser
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/conversations/user/:userId"
  params:
    - name: userId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/conversations/user/:userId"
      method: GET
      params:
        - name: userId
          value: ""
          type: path
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": "",
            "conversationType": "",
            "createdDate": ""
          }
