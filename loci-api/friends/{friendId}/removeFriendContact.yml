info:
  name: removeFriendContact
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/friends/:friendId"
  params:
    - name: friendId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/friends/:friendId"
      method: DELETE
      params:
        - name: friendId
          value: ""
          type: path
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "status": {
              "value": "",
              "connected": false,
              "notConnected": false
            }
          }
