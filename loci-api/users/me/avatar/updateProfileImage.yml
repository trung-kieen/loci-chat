info:
  name: updateProfileImage
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{baseUrl}}/users/me/avatar"
  body:
    type: json
    data: |-
      {
        "avatar": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/users/me/avatar"
      method: PATCH
      body:
        type: json
        data: |-
          {
            "avatar": ""
          }
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "userId": "",
            "emailAddress": "",
            "firstname": "",
            "lastname": "",
            "username": "",
            "profilePictureUrl": ""
          }
