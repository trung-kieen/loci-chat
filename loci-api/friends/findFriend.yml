info:
  name: findFriend
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/friends"
  params:
    - name: query
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/friends"
      method: GET
      params:
        - name: query
          value: ""
          type: query
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "friends": [
              {
                "userId": "",
                "fullname": "",
                "username": "",
                "email": "",
                "imageUrl": "",
                "friendshipStatus": ""
              }
            ]
          }
