info:
  name: patchUpdateGroupProfile
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/groups/:groupId"
  params:
    - name: groupId
      value: "{{group_id}}"
      type: path
  body:
    type: json
    data: |-
      {
        "groupName": "K",
        "profileImage": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/conversations/group"
      method: POST
      body:
        type: json
        data: |-
          {
            "groupName": "",
            "profileImage": ""
          }
    response:
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": "",
            "conversationType": "",
            "createdDate": ""
          }
